@model IEnumerable<ContosoUniversity.Models.Inscripcion>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Calificacion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Course)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Estudiante)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Calificacion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Course.CursoID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Estudiante.ID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.InscripcionID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.InscripcionID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.InscripcionID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
